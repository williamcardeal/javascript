//prompt

let wil = prompt("Tudo bem ?", "sim");

alert( `ok tudo bem ${wil}`)

//confirm

let isBoss = confirm("Vai jogar bola hoje?");

alert( isBoss );

/**Cobrimos 3 funções específicas do navegador para interagir com os visitantes:

alert
mostra uma mensagem.
prompt
mostra uma mensagem pedindo ao usuário para inserir texto. Retorna o texto ou, se o botão Cancelar ou Escfor clicado, null.
confirm
mostra uma mensagem e espera que o usuário pressione “OK” ou “Cancelar”. Ele retorna truepara OK e falsepara Cancel/ Esc. */

//podemos mudar o valor da variavel


